import { Panel } from "@components/shared/Panel";

export const NotesPanel = () => (
  <Panel id="notes-panel" title="Notes" defaultExpanded>
    <p class="empty-message">Nothing here yet.</p>
  </Panel>
);
